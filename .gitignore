# Foundry
cache/
out/
broadcast/

# Dependencies (Foundry libs at repo root only — NOT frontend/lib)
/lib/

# Env / keys (never commit real envs; keep the .example templates)
.env
.env.*
!.env.example
!**/.env.example
