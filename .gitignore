# Foundry
cache/
out/
broadcast/

# Dependencies (Foundry libs at repo root only — NOT frontend/lib)
/lib/

# Env / keys (never commit real envs; keep the .example templates)
.env
.env.*
!.env.example
!**/.env.example

# Oracle runtime state (ephemeral — rebuilt each boot, unlike the archive dirs)
data/poller-cursor.json
